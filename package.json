 {
  "name": "hairday",
  "description": "Aplicacao web para agendamento de corte de cabelo",
  "autor": "Pedro Benevides",
  "scripts": {
    "server": "json-server --watch server.json --port 3000"
  },

  "dependencies": {
    "json-server": "^1.0.0-alpha.21"
  }
}
